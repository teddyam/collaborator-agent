import { Client, ClientOptions } from '@microsoft/teams.common/http';
import { ChannelInfo } from '../models/channel-data/channel-info.js';
import { TeamDetails } from '../models/team-details.js';

declare class TeamClient {
    readonly serviceUrl: string;
    get http(): Client;
    set http(v: Client);
    protected _http: Client;
    constructor(serviceUrl: string, options?: Client | ClientOptions);
    getById(id: string): Promise<TeamDetails>;
    getConversations(id: string): Promise<ChannelInfo[]>;
}

export { TeamClient };
