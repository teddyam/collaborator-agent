import { Client, ClientOptions } from '@microsoft/teams.common/http';
import { Account } from '../../models/account.mjs';
import '../../models/role.mjs';

declare class ConversationMemberClient {
    readonly serviceUrl: string;
    get http(): Client;
    set http(v: Client);
    protected _http: Client;
    constructor(serviceUrl: string, options?: Client | ClientOptions);
    get(conversationId: string): Promise<Account[]>;
    getById(conversationId: string, id: string): Promise<Account>;
    delete(conversationId: string, id: string): Promise<void>;
}

export { ConversationMemberClient };
