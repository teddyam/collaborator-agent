'use strict';

var http = require('@microsoft/teams.common/http');
var signIn = require('./sign-in');
var token = require('./token');

class BotClient {
  token;
  signIn;
  get http() {
    return this._http;
  }
  set http(v) {
    this.token.http = v;
    this.signIn.http = v;
    this._http = v;
  }
  _http;
  constructor(options) {
    if (!options) {
      this._http = new http.Client();
    } else if ("request" in options) {
      this._http = options;
    } else {
      this._http = new http.Client(options);
    }
    this.token = new token.BotTokenClient(this.http);
    this.signIn = new signIn.BotSignInClient(this.http);
  }
}

exports.BotClient = BotClient;
Object.keys(signIn).forEach(function (k) {
  if (k !== 'default' && !Object.prototype.hasOwnProperty.call(exports, k)) Object.defineProperty(exports, k, {
    enumerable: true,
    get: function () { return signIn[k]; }
  });
});
Object.keys(token).forEach(function (k) {
  if (k !== 'default' && !Object.prototype.hasOwnProperty.call(exports, k)) Object.defineProperty(exports, k, {
    enumerable: true,
    get: function () { return token[k]; }
  });
});
//# sourceMappingURL=index.js.map
//# sourceMappingURL=index.js.map