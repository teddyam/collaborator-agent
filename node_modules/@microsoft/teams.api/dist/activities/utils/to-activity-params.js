'use strict';

var teams_cards = require('@microsoft/teams.cards');
var message = require('../message');

function toActivityParams(activity) {
  if (typeof activity === "string") {
    activity = {
      type: "message",
      text: activity
    };
  } else if (teams_cards.isAdaptiveCard(activity)) {
    activity = new message.MessageActivity().addCard("adaptive", activity);
  }
  return activity;
}

exports.toActivityParams = toActivityParams;
//# sourceMappingURL=to-activity-params.js.map
//# sourceMappingURL=to-activity-params.js.map