import { Fetch } from 'openai/core.mjs';
import { IAudioModel, AudioToTextParams, TextToAudioParams } from '@microsoft/teams.ai';
import { ILogger } from '@microsoft/teams.common/logging';

type OpenAIAudioPluginOptions = {
    readonly model: string;
    readonly apiKey?: string;
    readonly baseUrl?: string;
    readonly organization?: string;
    readonly project?: string;
    readonly headers?: {
        [key: string]: string;
    };
    readonly fetch?: Fetch;
    readonly timeout?: number;
    readonly logger?: ILogger;
};
declare class OpenAIAudioModel implements IAudioModel {
    readonly options: OpenAIAudioPluginOptions;
    private readonly _openai;
    private readonly _log;
    constructor(options: OpenAIAudioPluginOptions);
    audioToText(params: AudioToTextParams): Promise<string>;
    textToAudio(params: TextToAudioParams): Promise<Buffer<ArrayBuffer>>;
}

export { OpenAIAudioModel, type OpenAIAudioPluginOptions };
